# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CleanSwiftSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CleanSwiftSample
  pod 'SnapKit', '~> 5.0.0'
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
  pod 'Then'

  target 'CleanSwiftSampleTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble'
  end

  target 'CleanSwiftSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
